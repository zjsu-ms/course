server:
  port: 8080

spring:
  application:
    name: course-management-system

logging:
  level:
    com.zjgsu.course: INFO
    org.springframework.web: INFO
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"
